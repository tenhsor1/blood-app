'use strict';

describe('BloodApp.map module', function() {

  beforeEach(module('BloodApp.map'));

  describe('map controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mapCtrl = $controller('MapCtrl');
      expect(mapCtrl).toBeDefined();
    }));

  });
});